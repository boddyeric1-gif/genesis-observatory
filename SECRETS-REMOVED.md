# Secrets removed

The original export contained real Gemini and Tavily API keys plus a Netlify token.

Those have been stripped out of the cleaned `index.html` so the repo is safe to keep public.

AI features still work if *you* paste your own free keys into the `AI_CONFIG` block near the top of the script (search for `AI_CONFIG`). By default they are turned off.
